SendMailer

SendMailer is a simple Node.js + Express + Nodemailer app that lets users send emails through a web form using Gmail SMTP.

Features

Send emails from a form

Gmail SMTP support

Handles SSL/firewall issues

Simple UI

🛠 Tech

Node.js, Express, Nodemailer, EJS
